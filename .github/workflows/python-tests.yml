name: Tests, Build & Release

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test-build:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.version.outputs.tag }}
      prerelease: ${{ steps.version.outputs.prerelease }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - uses: astral-sh/setup-uv@v9
        with:
          version: "0.11.32"
          enable-cache: true
      - name: Install dependencies
        run: uv sync --locked
      - name: Validate source version and release tag
        id: version
        env:
          REF_TYPE: ${{ github.ref_type }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          args=(--check-only --github-output "$GITHUB_OUTPUT")
          if [[ "$REF_TYPE" == tag ]]; then
            args+=(--tag "$REF_NAME")
          fi
          uv run python scripts/release.py "${args[@]}"
      - name: Run tests
        run: uv run python -m pytest -q
      - name: Build and verify release assets
        env:
          RELEASE_TAG: ${{ steps.version.outputs.tag }}
        run: uv run python scripts/release.py --tag "$RELEASE_TAG"
      - name: Upload tested assets
        uses: actions/upload-artifact@v4
        with:
          name: mdl-${{ steps.version.outputs.tag }}
          path: |
            dist/release/mdl.py
            dist/release/pyproject.toml
            dist/release/uv.lock
            dist/release/LICENSE
            dist/release/RELEASE_NOTES.md
            dist/release/INSTALL.txt
            dist/release/SHA256SUMS
          if-no-files-found: error

  release:
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: test-build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download tested assets
        uses: actions/download-artifact@v4
        with:
          name: mdl-${{ needs.test-build.outputs.tag }}
          path: assets
      - name: Verify assets
        working-directory: assets
        run: sha256sum --check SHA256SUMS
      - name: Publish versioned release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ needs.test-build.outputs.tag }}
          PRERELEASE: ${{ needs.test-build.outputs.prerelease }}
        run: |
          args=(--verify-tag --title "MDL $RELEASE_TAG" --notes-file assets/RELEASE_NOTES.md --generate-notes)
          if [[ "$PRERELEASE" == true ]]; then
            args+=(--prerelease --latest=false)
          fi
          gh release create "$RELEASE_TAG" assets/mdl.py assets/pyproject.toml assets/uv.lock \
            assets/LICENSE assets/INSTALL.txt assets/RELEASE_NOTES.md assets/SHA256SUMS "${args[@]}"
